// run build on all packages
// compose them using electron's build process
// maybe minify or use separate production builds
// package them all together and shoot it out

// electron needs to load from an internal index.html instead of from localhost for production builds
// the interface needs to be packaged for electron-renderer
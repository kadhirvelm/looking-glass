// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function mergeDatasets(
  datasetNames: string[],
  outputDatasetName?: string
): string {
  console.log("REACHING HERE", datasetNames, outputDatasetName);
  // write the new dataset with metadata
  // for each dataset
  // read the dataset into memory
  // append its data to the data field
  // write to the dataset provenance
  // write the row provenance
  return "Sample dataset.json";
}
